{
  "name": "kimichat",
  "version": "1.0.0",
  "description": "KimiChat – AI-Powered Chat Universe",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build && rm -rf ../public && mkdir ../public && cp -r dist/* ../public"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
